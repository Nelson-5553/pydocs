---
title: Input and Output
description: Input and Output Guide
sidebar:
  order: 1
---

import { Aside } from "@astrojs/starlight/components"
import { Code } from "@astrojs/starlight/components"


When you write a program, not everything should stay within the code. Sometimes you need to show information to the user (output), and other times ask for data (input). In Python, this is mainly done with two very simple functions:

- `print()` to display messages or results.

- `input()` to receive data from the user.

With these two functions you can start building interactive programs.

## print()

The `print()` function in Python is used to display information in the console. It allows you to print text, numbers, variables, and more, using simple syntax.

<Code 
code="print('Welcome to PyDocs')" 
lang="py" 
title="print.py" 
mark="highlights" />

Output:

<Code 
code="
Welcome to PyDocs
"
title="terminal" 
mark="highlights"
frame='terminal' />

<Aside type="tip" title="Challenge">

Try running it yourself in Google Colab.<br />
<a href="https://colab.research.google.com/github/AxelSchenfeld/pydocs/blob/docs/notebooksyarreglosimputouput/src/content/es_notebooks/syntax-basic/Input-and-Output_print.ipynb" target="_blank">
  Run in Google Colab 🚀
</a>
<br />
Why don't you try printing Hello World in Google Colab!?

</Aside>

## input()

The `input()` function in Python asks the user to enter a value. Then, it reads what the user enters and always returns it as a string.

<Code 
code="name = input('What is your name? ')
print('Hello, ' + name + '!')" 
lang="py" 
title="input.py" 
mark="highlights" />

<Aside type="tip" title="Challenge">

Try running this on your computer to see what it shows
</Aside>
