---
title: Type Conversion
description: Guide to Type Conversion
sidebar:
  order: 2
---

import { Code } from "@astrojs/starlight/components"
import { Aside } from "@astrojs/starlight/components"

In Python, data types are used to classify a specific type of data, determine the values you can assign to the type, and the operations you can perform on it. When performing programming tasks, sometimes you will need to apply value conversions between types to manipulate the values differently. For example, we may need to concatenate numeric values with strings or represent decimal places in numbers that started as integer values.

The types are:

- `int()`
- `float()`
- `str()`
- `bool()`
- `list()`
- `tuple()`
- `set()`
- `dict()`

### `int()`

Converts a numeric value or a string that represents an integer to the int data type. If the value is a decimal number, it rounds it down (removes the decimal places). If it is not numeric, it throws an error.

<Code
code={`
print(int(43.54)) # Converts the decimal to integer, removing the decimal places
`}
lang="py"
title="Int.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
43
`}
title="Int.py"
mark="highlights"
frame='terminal' />

<Aside type="note">
  You cannot directly convert a string representing a decimal number (float) to an integer using int(). For example, int("3.14") raises ValueError.
</Aside>

### `float()`

Converts an integer or a string that represents a number to a decimal number (float). If it is not numeric, it throws an error. Useful when you need to work with fractional values.
<Code
code={`
print(float('3.14'))
print(float(10))
`}
lang="py"
title="Float.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
3.14
10.0
`}
title="float.py"
mark="highlights"
frame='terminal' />

### `str()`

Converts any data type (integer, decimal, boolean, lists, etc.) to a string (str). It is useful for displaying messages or concatenating with other texts.
<Code
code={`
print(str(100))
print(str(True))
`}
lang="py"
title="Str.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
100
True
`}
title="Str.py"
mark="highlights"
frame='terminal' />

### `bool()`
Converts a value to True or False depending on whether it is "empty" or not. Values such as 0, "" (empty string), [] (empty list), None and False are considered False. Everything else is True.
<Code
code={`
print(bool(1))
print(bool(0))
print(bool('Hello'))
`}
lang="py"
title="Bool.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
True
False
True
`}
title="Bool.py"
mark="highlights"
frame='terminal' />

### `list()`
Converts iterable objects (such as strings, tuples, sets) into a list.
<Code
code={`
print(list('hello'))
print(list((1, 2, 3)))
`}
lang="py"
title="List.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
['h', 'e', 'l', 'l', 'o']
[1, 2, 3]
`}
title="List.py"
mark="highlights"
frame='terminal' />

### `tuple()`

Converts an iterable object into a tuple. Similar to list(), but the result is immutable (cannot be modified).

<Code
code={`
print(tuple('abc'))
print(tuple([1, 2, 3]))
`}
lang="py"
title="Tuple.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
('a', 'b', 'c')
(1, 2, 3)
`}
title="Tuple.py"
mark="highlights"
frame='terminal' />

### `set()`

set() converts an iterable object into a set, removing duplicates and without maintaining a specific order (it is mutable).

<Code
code={`
print(set([1, 2, 2, 3]))
print(set('hello'))
`}
lang="py"
title="Set.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
{1, 2, 3}
{'h', 'e', 'l', 'o'}
`}
title="Set.py"
mark="highlights"
frame='terminal' />

<Aside type="note">
  For set() to work, the elements of the iterable must be hashable, which means they must have a unique hash value.
  Examples of non-hashable objects:
  - Lists
  - Sets
  - Dictionaries
</Aside>

### `dict()`

Converts an iterable object into a dictionary.

<Code
code={`
print(dict([('a', 1), ('b', 2)]))
print(dict(a=1, b=2))
`}
lang="py"
title="Dict.py"
mark="highlights"
frame='auto' />
Output:
<Code
code={`
{'a': 1, 'b': 2}
{'a': 1, 'b': 2}
`}
title="Dict.py"
mark="highlights"
frame='terminal' />

<Aside type='note'>
  dict() only works in the following cases:
  - Iterable objects containing key-value pairs.
  - Iterable objects containing tuples, lists, or sets of two elements.
  - Iterable objects containing dictionaries.
  - Keys must be unique and hashable.
</Aside>
