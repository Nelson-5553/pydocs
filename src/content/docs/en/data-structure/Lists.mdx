---
title: Lists
description: Lists Guide
sidebar:
  order: 1
---
import { Code } from "@astrojs/starlight/components"
import { Aside } from "@astrojs/starlight/components"

In Python, a list is a flexible data structure that allows you to store an ordered collection of elements of any type. They are mutable, which means they can be modified after creation. They are defined with square brackets [] and their elements are separated by commas.

## Creating a list

Let's make a list of fruits

<Code 
code="
# List of fruits
banana
print(fruits)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Output:

<Code 
code="
['apple', 'banana', 'orange', 'pear', 'grape', 'mango']
"
title="terminal" 
mark="highlights"
frame='terminal' />

## Indexing

Suppose we only want a particular element from the fruits list. With this in mind, you should know that each element in a list has an index with which we can locate each element of a list, and for this we must carry out the concept of Indexing as follows

<Code 
code="
# List of fruits
fruits = ['apple', 'banana', 'orange', 'pear', 'grape', 'mango']
print(fruits[1])
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
banana
"
title="terminal" 
mark="highlights"
frame='terminal' />


<Aside type="note">
In Python, the first index **is always 0**. This means that the first element of a tuple or list is accessed with `[0]`, not with `[1]`.
</Aside>


## Modifying an element

Through indexing we can modify a particular element of the list

<Code 
code="
# List of fruits
fruits = ['apple', 'banana', 'orange', 'pear', 'grape', 'mango']

# We assign a new element to the element at position 1
fruits[1] = 'strawberry'  
print(fruits)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
['apple', 'strawberry', 'orange', 'pear', 'grape', 'mango']
"
title="terminal" 
mark="highlights"
frame='terminal' />

## Adding elements

To add an element to a list you must use the `.append()` function

<Code 
code="
# List of fruits
fruits = ['apple', 'banana', 'orange', 'pear', 'grape', 'mango']

# New element added to the end of the list
fruits.append('strawberry')
print(fruits)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
['apple', 'banana', 'orange', 'pear', 'grape', 'mango', 'strawberry']
"
title="terminal" 
mark="highlights"
frame='terminal' />

## Removing an element

To remove an element from a list you must use the `.remove()` function

<Code 
code="
# List of fruits
fruits = ['apple', 'banana', 'orange', 'pear', 'grape', 'mango']

# Remove selected element
fruits.remove('mango')
print(fruits)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
['apple', 'banana', 'orange', 'pear', 'grape']
"
title="terminal" 
mark="highlights"
frame='terminal' />

<Aside type="tip" title='Challenge'>
Why don't you try to see what the `.pop()` and `.insert()` functions do
</Aside>
