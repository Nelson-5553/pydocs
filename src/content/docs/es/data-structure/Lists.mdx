---
title: Listas
description: Guía para Listas
sidebar:
  order: 1
---
import { Code } from "@astrojs/starlight/components"
import { Aside } from "@astrojs/starlight/components"

En Python, una lista es una estructura de datos flexible que permite almacenar una colección ordenada de elementos de cualquier tipo. Son mutables, lo que significa que se pueden modificar después de su creación. Se definen entre corchetes [], pueden tener distintos tipos de datos y sus elementos se separan por comas. 

## Crear una lista

Hagamos una lista de frutas

<Code 
code="
# Lista de frutas
frutas = ['manzana', 'banana', 'naranja', 'pera', 'uva', 'mango']
print(frutas)

# Lista con distintos tipos de datos
distintos_tipos = [1, 2.5, 'hola', True]
print(distintos_tipos)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
['manzana', 'banana', 'naranja', 'pera', 'uva', 'mango']
[1, 2.5, 'hola', True]
"
title="terminal" 
mark="highlights"
frame='terminal' />

## Indexación

Supongamos que queremos acceder a un elemento específico de la lista de frutas. Cada elemento de la lista tiene un índice, que nos permite ubicarlo mediante indexación, como se muestra a continuación:

<Code 
code="
# Lista de frutas
frutas = ['manzana', 'banana', 'naranja', 'pera', 'uva', 'mango']

print(frutas[1])  
print(frutas[-1])  

"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
banana
mango
"
title="terminal" 
mark="highlights"
frame='terminal' />


<Aside type="note">
- En Python, el primer índice **siempre es 0**. Esto significa que el primer elemento de una tupla o lista se accede con `[0]`, no con `[1]`.
- En Python, los índices negativos se utilizan para acceder a elementos desde el final de la lista. Por ejemplo, `-1` se refiere al último elemento, `-2` al penúltimo, y así sucesivamente.
</Aside>


## Modificar un elemento

A través de la indexación podemos modificar un elemento de la lista en particular.

<Code 
code="
# Lista de frutas
frutas = ['manzana', 'banana', 'naranja', 'pera', 'uva', 'mango']

# Asignamos un nuevo elemento al elemento que se encuentra en la posicion 1
frutas[1] = 'fresa'  
print(frutas)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
['manzana', 'fresa', 'naranja', 'pera', 'uva', 'mango']
"
title="terminal" 
mark="highlights"
frame='terminal' />

## Añadir elementos

Para añadir un elemento a una lista debes usar el método `.append()`

<Code 
code="
# Lista de frutas
frutas = ['manzana', 'banana', 'naranja', 'pera', 'uva', 'mango']

# Nuevo elemento añadido al final de la lista
frutas.append('fresa')
print(frutas)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
['manzana', 'banana', 'naranja', 'pera', 'uva', 'mango', 'fresa']
"
title="terminal" 
mark="highlights"
frame='terminal' />

## Eliminar un elemento

Para eliminar un elemento a una lista debes usar el método `.remove()`

<Code 
code="
# Lista de frutas
frutas = ['manzana', 'banana', 'naranja', 'pera', 'uva', 'mango']

# Remover elemento seleccionado
frutas.remove('mango')
print(frutas)
"
lang="py" 
title="list.py" 
mark="highlights"
frame='auto' />

Salida:

<Code 
code="
['manzana', 'banana', 'naranja', 'pera', 'uva']
"
title="terminal" 
mark="highlights"
frame='terminal' />

<Aside type="tip" title='Reto'>
¿Por qué no intentas ver que hacen los métodos de `.pop()` y `.insert()`?
</Aside>
