---
title: Entradas y Salida
description: Guía para Entradas y Salida
sidebar:
  order: 1
---

import { Aside } from "@astrojs/starlight/components"
import { Code } from "@astrojs/starlight/components"


Cuando escribes un programa, no todo debe quedar dentro del código. A veces necesitas mostrar información al usuario (salida), y otras veces pedirle datos (entrada).En Python, esto se hace principalmente con dos funciones muy sencillas:

- <code>print()</code> para mostrar mensajes o resultados.

- <code>input()</code> para recibir datos del usuario.

Con estas dos funciones puedes empezar a construir programas interactivos.

## Print()

La función <code>print()</code> en Python se usa para mostrar información en la consola. Permite imprimir texto, números, variables, y más, utilizando una sintaxis sencilla.

<Code 
code="print('Bienvenido a PyDocs') # Respuesta en consola  print('Bienvenido a PyDocs')" 
       
lang="py" 
title="print.py" 
mark="highlights" />

<Aside type="tip" title="Reto">

¿Por que no intentas Imprimir Hola Mundo!?

```python
  print("Hola Mundo!")
```
</Aside>

## Input()

La función <code>input()</code> en Python solicita al usuario que introduzca un valor. Luego, lee lo que el usuario introduce y lo devuelve como una cadena 

<Code 
code="nombre = input('¿Cuál es tu nombre? ')
          print('Hola, ' + nombre + '!')" 
       
lang="py" 
title="print.py" 
mark="highlights" />

<Aside type="tip" title="Reto">

¿Intenta ejecutar esto en tu Equipo a ver que te muestra?

</Aside>