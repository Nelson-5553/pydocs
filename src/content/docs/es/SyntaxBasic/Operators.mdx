---
title: Operadores
description: Guía para Operadores
sidebar:
  order: 4
---

import PyTable from "@components/PyTable.astro"
import { Code } from "@astrojs/Starlight/components"
import { Aside } from "@astrojs/Starlight/components"

Un operador es un símbolo o palabra clave que le dice a Python que realice una operación específica sobre uno o más valores (también llamados operandos). Los operadores permiten realizar tareas como sumar números, comparar valores, asignar resultados a variables o evaluar condiciones lógicas.

## Tipos de opéradores

En python al igual que en otros lenguajes existen una variedad de tipos de operadores los cuales les enseñaremos a continuacion

### Operadores Aritméticos

Son los operadores más básicos y se utilizan para realizar operaciones matemáticas como suma, resta, multiplicación o división. Python los usa para trabajar con números de manera directa.

<PyTable 
  headers={["Operador", "Descripción"]}
  rows={[
    ["+", "Suma"],
    ["-", "Resta"],
    ["*", "Multiplicación"],
    ["/", "División (resultado flotante)"],
    ["//", "División entera "],
    ["%", "Módulo (residuo) "],
    ["**", "Potencia"],
    // más filas...
  ]}
/>

#### Ejemplo de codigo

<Code 
code="x = 12 
      y = 24
      
      print(x + y)   # Resultado: 36
      print(x - y)   # Resultado: -12
      print(x * y)   # Resultado: 288
      print(x / y)   # Resultado: 0.5
      print(x // y)  # Resultado: 0
      print(x % y)   # Resultado: 12
      " 
       
lang="py" 
title="Operators.py" 
mark="highlights" />

<Aside type="tip" title="Reto">

```python
# ¿Qué número obtienes al usar x ** y?
# Pista: es 12 elevado a 24. ¡Muy grande!
```
</Aside>

### Operadores de Comparación

Estos operadores permiten comparar dos valores. El resultado de una comparación es siempre un valor booleano: True si la condición se cumple, y False si no.

<PyTable 
  headers={["Operador", "Descripción"]}
  rows={[
    ["==", "Igual a"],
    ["!=", "Distinto de "],
    [">", "Mayor que "],
    ["<", "Menor que         "],
    [">=", "Mayor o igual que"],
    ["<=", "Menor o igual que  "],
    // más filas...
  ]}
/>

#### Ejemplo de codigo

<Code 
code="      
      print(5 == 5)   # Resultado: True
      print(5 != 3)   # Resultado: True
      print(7 > 3)   # Resultado: True
      print(2 < 4)   # Resultado: True
      print(6 >= 6)  # Resultado: True
      print(3 <= 5)   # Resultado: True
      " 
       
lang="py" 
title="Operators.py" 
mark="highlights" />

### Operadores de Asignación

<PyTable 
  headers={["Operador", "Descripción"]}
  rows={[
    ["=", "Asignación"],
    ["+=", "Suma y asignación"],
    ["-=", "Resta y asignación"],
    ["*=", "Multiplicación y asignación"],
    ["/=", "División y asignación"],
    ["%=", "Módulo y asignación "],
    ["//=", "División entera y asignación"],
    ["**=", "Potencia y asignación "],
    
    // más filas...
  ]}
/>

#### Ejemplo de codigo

<Code 
code="      
      print(x = 5)   # Asigna el valor 5 a x
      print(x += 3)   # x = x + 3
      print(x -= 2)   # x = x - 2
      print(x *= 4)   # x = x * 4
      print(x /= 2)   # x = x / 2
      print(x %= 3)  # x = x % 3
      print(x //= 2)   # x = x // 2
      print(x **= 3)   # x = x ** 3
      " 
       
lang="py" 
title="Operators.py" 
mark="highlights" />

### Operadores Lógicos 

Sirven para combinar múltiples condiciones y obtener un resultado booleano. Son usualemente usados en estructuras de control como los condicionales

<PyTable 
  headers={["Operador", "Descripción", "Ejemplo", "Resultado"]}
  rows={[
    ["and", "Verdadero si ambas condiciones son verdaderas ", "True and False", "False"],
    ["or", "Verdadero si al menos una condición es verdadera","True or False","True"],
    ["not", "Invierte el valor de la condición", "not True","False"],   
    // más filas...
  ]}
/>