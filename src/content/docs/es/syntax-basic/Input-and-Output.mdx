---
title: Entrada y Salida
description: Guía para Entrada y Salida
sidebar:
  order: 1
---

import { Aside } from "@astrojs/starlight/components"
import { Code } from "@astrojs/starlight/components"


Cuando escribes un programa, no todo debe quedar dentro del código. A veces necesitas mostrar información al usuario (salida), y otras veces pedirle datos (entrada). En Python, esto se hace principalmente con dos funciones muy sencillas:

- `print()` para mostrar mensajes o resultados.

- `input()` para recibir datos del usuario.

Con estas dos funciones puedes empezar a construir programas interactivos.

## print()

La función `print()` en Python se usa para mostrar información en la consola. Permite imprimir texto, números, variables, y más, utilizando una sintaxis sencilla.

<Code 
code="print('Bienvenido a PyDocs')" 
       
lang="py" 
title="print.py" 
mark="highlights" />

Salida:

<Code 
code="
'Bienvenido a PyDocs'
"
title="terminal" 
mark="highlights"
frame='terminal' />

<Aside type="tip" title="Reto">

¿Por qué no intentas Imprimir Hola Mundo!?

```python
  print("Hola Mundo!")
```
</Aside>

## input()

La función `input()` en Python solicita al usuario que introduzca un valor. Luego, lee lo que el usuario introduce y lo devuelve siempre como una cadena. 

<Code 
code="nombre = input('¿Cuál es tu nombre?')
          print('Hola, ' + nombre + '!')" 
       
lang="py" 
title="input.py" 
mark="highlights" />

<Aside type="tip" title="Reto">

¿Intenta ejecutarlo en tu equipo para ver qué muestra?

</Aside>